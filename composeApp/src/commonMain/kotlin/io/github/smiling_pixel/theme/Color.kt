package io.github.smiling_pixel.theme

import androidx.compose.ui.graphics.Color

// Light Theme Colors

/** Primary color for the light theme, representing the main green brand. */
val primaryLight = Color(0xFF386B36)
/** Color used for text/icons on top of the primary color in the light theme. */
val onPrimaryLight = Color(0xFFFFFFFF)
/** A lighter tone of the primary color, used as a background for grouped elements in the light theme. */
val primaryContainerLight = Color(0xFFB9F3B1)
/** Color used for text/icons on top of the primary container in the light theme. */
val onPrimaryContainerLight = Color(0xFF002203)

/** Secondary brand color for the light theme, used for less prominent accents. */
val secondaryLight = Color(0xFF526350)
/** Color used for text/icons on top of the secondary color in the light theme. */
val onSecondaryLight = Color(0xFFFFFFFF)
/** A lighter tone of the secondary color, used for secondary grouped elements in the light theme. */
val secondaryContainerLight = Color(0xFFD5E8D0)
/** Color used for text/icons on top of the secondary container in the light theme. */
val onSecondaryContainerLight = Color(0xFF111F0F)

/** Tertiary brand color for the light theme, providing a contrasting accent (teal/blue). */
val tertiaryLight = Color(0xFF39656B)
/** Color used for text/icons on top of the tertiary color in the light theme. */
val onTertiaryLight = Color(0xFFFFFFFF)
/** A lighter tone of the tertiary color, used for tertiary grouped elements in the light theme. */
val tertiaryContainerLight = Color(0xFFBCEBF2)
/** Color used for text/icons on top of the tertiary container in the light theme. */
val onTertiaryContainerLight = Color(0xFF001F23)

/** Error color for the light theme, indicating destructive actions or errors. */
val errorLight = Color(0xFFBA1A1A)
/** Color used for text/icons on top of the error color in the light theme. */
val onErrorLight = Color(0xFFFFFFFF)
/** A lighter tone of the error color, used as a subtle error background in the light theme. */
val errorContainerLight = Color(0xFFFFDAD6)
/** Color used for text/icons on top of the error container in the light theme. */
val onErrorContainerLight = Color(0xFF410002)

/** Background color for the overall application layout in the light theme. */
val backgroundLight = Color(0xFFF7FBF2)
/** Color used for text/icons on top of the overall background in the light theme. */
val onBackgroundLight = Color(0xFF181D17)
/** Surface color for components like cards and menus in the light theme. */
val surfaceLight = Color(0xFFF7FBF2)
/** Color used for text/icons on top of surfaces in the light theme. */
val onSurfaceLight = Color(0xFF181D17)


// Dark Theme Colors

/** Primary color for the dark theme, optimized for dark backgrounds. */
val primaryDark = Color(0xFF9ED697)
/** Color used for text/icons on top of the primary color in the dark theme. */
val onPrimaryDark = Color(0xFF05390C)
/** A darker tone of the primary color, used as a background for grouped elements in the dark theme. */
val primaryContainerDark = Color(0xFF1F5120)
/** Color used for text/icons on top of the primary container in the dark theme. */
val onPrimaryContainerDark = Color(0xFFB9F3B1)

/** Secondary brand color for the dark theme. */
val secondaryDark = Color(0xFFB9CCB5)
/** Color used for text/icons on top of the secondary color in the dark theme. */
val onSecondaryDark = Color(0xFF253424)
/** A darker tone of the secondary color for the dark theme. */
val secondaryContainerDark = Color(0xFF3B4B39)
/** Color used for text/icons on top of the secondary container in the dark theme. */
val onSecondaryContainerDark = Color(0xFFD5E8D0)

/** Tertiary brand color for the dark theme. */
val tertiaryDark = Color(0xFFA0CFD5)
/** Color used for text/icons on top of the tertiary color in the dark theme. */
val onTertiaryDark = Color(0xFF00363B)
/** A darker tone of the tertiary color for the dark theme. */
val tertiaryContainerDark = Color(0xFF1E4D53)
/** Color used for text/icons on top of the tertiary container in the dark theme. */
val onTertiaryContainerDark = Color(0xFFBCEBF2)

/** Error color for the dark theme. */
val errorDark = Color(0xFFFFB4AB)
/** Color used for text/icons on top of the error color in the dark theme. */
val onErrorDark = Color(0xFF690005)
/** A darker tone of the error color for the dark theme. */
val errorContainerDark = Color(0xFF93000A)
/** Color used for text/icons on top of the error container in the dark theme. */
val onErrorContainerDark = Color(0xFFFFDAD6)

/** Background color for the overall application layout in the dark theme. */
val backgroundDark = Color(0xFF10140F)
/** Color used for text/icons on top of the overall background in the dark theme. */
val onBackgroundDark = Color(0xFFE0E4DB)
/** Surface color for components like cards and menus in the dark theme. */
val surfaceDark = Color(0xFF10140F)
/** Color used for text/icons on top of surfaces in the dark theme. */
val onSurfaceDark = Color(0xFFE0E4DB)

/** Pure black background for OLED screens in the dark theme. */
val backgroundPureBlack = Color(0xFF000000)
/** Pure black surface for OLED screens in the dark theme. */
val surfacePureBlack = Color(0xFF000000)
